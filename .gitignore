# Progress backups exported from the apps (personal reading state)
# Covers xmen-reading-progress.json and spider-man-omnibus-progress.json,
# plus whatever a future tracker names its export.
*progress*.json

# Scratch
/tmp/
*.log
.DS_Store

# Full-size cover downloads, cached so tools/fetch_covers.py can be rerun cheaply
tools/.cover-cache/
