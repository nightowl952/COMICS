"""Guided tour content, one module per hero.

Kept out of the tracker HTML for the same reason a shelf's OMNI is: the page is
generated from a source you can actually edit. `build_tours.py` splices the
module's TOUR dict in as `const TOUR = {...};`.

Nothing here is model-generated. Every claim is researched, and where the
community's view of a book is reported it is reported as the community's view
rather than as fact.
"""
